<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="?attr/colorSurface"
        tools:context=".view.fragment.PhotoFragment">

        <TextView
            android:id="@+id/photoActivityTopbarText"
            style="@style/TopBarTextStyle2"
            android:text="@string/photo_title"
            app:layout_constraintBottom_toBottomOf="@id/photoBackButton"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="@id/photoBackButton" />

        <ImageView
            android:id="@+id/photoBackButton"
            style="@style/tinyButtonStyle"
            android:layout_marginStart="@dimen/large_horizontal_spacing"
            android:layout_marginTop="@dimen/medium_vertical_spacing"
            android:src="@drawable/arrow_back"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <ImageView
            android:id="@+id/photoImageView"
            android:layout_width="match_parent"
            android:layout_height="@dimen/match_constraint_size"
            android:layout_marginStart="@dimen/large_horizontal_spacing"
            android:layout_marginTop="@dimen/large_vertical_spacing"
            android:layout_marginEnd="@dimen/large_horizontal_spacing"
            android:layout_marginBottom="@dimen/huge_vertical_spacing"
            android:src="@drawable/base_settings_photo"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/photoActivityTopbarText" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>
